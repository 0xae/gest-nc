{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Classroom list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Capacity</th>
                <th>Name</th>
                <th>Createdat</th>
                <th>Available</th>
                <th>Createdby</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('administration_classroom_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.capacity }}</td>
                <td>{{ entity.name }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.available }}</td>
                <td>{{ entity.createdBy }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('administration_classroom_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('administration_classroom_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('administration_classroom_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
