{% extends 'BackendBundle:Default:layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Editar Reclamação Interna</h3>

                    <p style="font-size:12px;color:gray">
                        <span class="fa fa-user"></span>
                        Criada por {{entity.createdBy.name}}/{{entity.createdBy.entity.name}}
                    </p>

                    <p>
                        <a href="{{ path('administration_IReclamation') }}">
                            <span class="glyphicon glyphicon-back"></span>Voltar
                        </a>
                    </p>
                </div>
                <!-- /.box-header -->

                <div class="box-body">
                    <div role="tabpanel">
                        <!-- Nav tabs -->
                        <ul class="nav nav-tabs" role="tablist">
                            <li role="presentation" class="active">
                                <a href="#home" aria-controls="home" role="tab" data-toggle="tab">Dados</a>
                            </li>
                            <li role="presentation">
                                <a href="#profile" aria-controls="profile" role="tab" data-toggle="tab">
                                    <span class="fa fa-file"></span> Anexos
                                </a>
                            </li>
                        </ul>

                        <!-- Tab panes -->
                        <div class="tab-content" style="padding: 20px">
                            <div role="tabpanel" class="tab-pane active" id="home">
                                {{
                                    include('BackendBundle:IReclamation:form.html.twig', {
                                        'form' : edit_form
                                    })
                                }}
                            </div>

                            <div role="tabpanel" class="tab-pane" id="profile">
                                <h1>Tab 2</h1>
                            </div>
                        </div>

                    </div>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
    </div>
{% endblock %}

