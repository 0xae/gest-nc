<table class="table table-hover table-striped">
    <thead>
        <tr>                                        
            <th>Nome</th>
            <th>Codigo</th>
            <th>N&iacute;vel</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for c in courses %}
            <tr>                                                                                    
                <td>
                    <a href="{{ path('administration_professor_show', { 'id': c.id }) }}">
                        {{ c.name }}
                    </a>
                </td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
