{% extends 'BackendBundle:Default:layout.html.twig' %}

{% block content -%}

    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Listagem de Complaint</h3>

                    <div class="box-tools">
                        <a class="btn btn-primary" 
                        href="{{ path('administration_Complaint_new') }}">
                            <span class="glyphicon glyphicon-plus"></span>
                            Criar Complaint
                        </a>
                    </div>
                </div>
                <!-- /.box-header -->

                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover table-striped">
                        <thead>
                            <tr>                                        <th>ID#</th>
                                    <th>Name</th><th>Address</th><th>Locality</th><th>Phone</th><th>Email</th><th>Type</th><th>Opname</th><th>Opaddress</th><th>Oplocality</th><th>Opphone</th><th>Opemail</th><th>Factdate</th><th>Factannex</th><th>Factdetail</th><th>Hasproduct</th><th>Annex</th><th>Annextype</th><th>Createdat</th><th>Createdby</th>                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities %}
                                <tr>                                                                                    <td>
                                                <a href="{{ path('administration_Complaint_show', { 'id': entity.id }) }}">
                                                #{{ entity.id }}
                                                </a>
                                            </td>                                                                                    <td>{{ entity.name }}</td>                                                                                    <td>{{ entity.address }}</td>                                                                                    <td>{{ entity.locality }}</td>                                                                                    <td>{{ entity.phone }}</td>                                                                                    <td>{{ entity.email }}</td>                                                                                    <td>{{ entity.type }}</td>                                                                                    <td>{{ entity.opName }}</td>                                                                                    <td>{{ entity.opAddress }}</td>                                                                                    <td>{{ entity.opLocality }}</td>                                                                                    <td>{{ entity.opPhone }}</td>                                                                                    <td>{{ entity.opEmail }}</td>                                                                                    <td>{% if entity.factDate %}{{ entity.factDate|date('Y-m-d H:i:s') }}{% endif %}</td>                                                                                    <td>{{ entity.factAnnex }}</td>                                                                                    <td>{{ entity.factDetail }}</td>                                                                                    <td>{{ entity.hasProduct }}</td>                                                                                    <td>{{ entity.annex }}</td>                                                                                    <td>{{ entity.annexType }}</td>                                                                                    <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>                                                                                    <td>{{ entity.createdBy }}</td>                                            <td><a href="{{ path('administration_Complaint_edit', { 'id': entity.id }) }}">
<span class="badge badge-primary">
        EDITAR
</span>
</a>
                                            </td>                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                                    </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
    </div>

{% endblock %}
