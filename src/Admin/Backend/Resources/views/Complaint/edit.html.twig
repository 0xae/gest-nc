{% extends 'BackendBundle:Default:layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title" style="font-weight:bold">
                        {% if entity.type == 'queixa' %}
                            Queixa
                        {% else %}
                            Den&uacute;ncia
                        {% endif %}

                        <a href="#">
                        #{{ entity.objCode }}
                        </a>
                    </h3>
                    <p style="font-size:12px;color:gray">
                        <span class="fa fa-user"></span>
                        Criada por {{entity.createdBy.name}}/{{entity.createdBy.entity.name}}
                    </p>
                    <p>
                        <a href="{{ path('administration_Complaint') }}">                        
                            <span class="fa fa-back"></span> Voltar
                        </a>
                    </p>
                </div>
                <!-- /.box-header -->

                <div class="box-body">
                    {{ include('BackendBundle:Complaint:form.html.twig', {'form' : edit_form}) }}                    
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
    </div>
{% endblock %}
