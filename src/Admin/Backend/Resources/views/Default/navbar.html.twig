<!-- Logo -->
<a href="#" class="logo" style="background-color: #fff;">
    <!-- mini logo for sidebar mini 50x50 pixels -->
    <span class="logo-lg">
        {% image '@BackendBundle/Resources/public/img/arfa.jpg' %}
            <img src="{{ asset_url }}" class="user-image" width="100" alt="User Image!" />
        {% endimage %}
    </span>

    <!-- logo for regular state and mobile devices -->
    <span class="logo-mini" style="color: #000">
        ARFA
    </span>
</a>

<!-- Header Navbar: style can be found in header.less -->
<nav class="navbar navbar-static-top bg-green" role="navigation">
    <div class="row">
        <div class="col-md-7">
            <!-- Sidebar toggle button-->
            <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
                <span class="sr-only">Toggle navigation</span>
                <span style="font-weight: bold;font-size: 16px;">
                    Gestão de Ocorrências Arfa
                </span>
            </a>
            
            <div class="">
                <h3 class="app-title">
                {# Gest&atilde;o de Den&uacute;ncias, Queixas, Reclama&ccedil;&otilde;es e Sugest&otilde;es</h3> #}
                {# GESTÃO DE DENÚNCIAS, QUEIXAS, RECLAMAÇÕES E SUGESTÕES #}
                <!-- Navbar Right Menu -->
                </h3>
            </div>
        </div>

        <div class="col-md-4 pull-right"> 
            <div class="navbar-custom-menu">
                <ul class="nav navbar-nav">
                    {# <!-- Messages: style can be found in dropdown.less-->
                    <li class="dropdown messages-menu">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="fa fa-envelope-o"></i>
                            <span class="label label-success">4</span>
                        </a>
                        <ul class="dropdown-menu">
                            <!-- User image -->
                            <li class="user-header">
                                <p>
                                    <small>{{ 'layout.logged_in_as'|trans({'%username%': app.user.lastlogin|date("d-m-y")}, 'FOSUserBundle') }}</small>
                                </p>
                            </li>

                            <!-- Menu Footer-->
                            <li class="user-footer">
                            <div class="pull-left">
                                <a href="{{ path('fos_user_profile_show') }}" class="btn btn-default btn-flat">Perfil</a>
                            </div>

                            <div class="pull-right">
                                <a href="{{ path('fos_user_security_logout') }}" class="btn btn-default btn-flat">Sair</a>
                            </div>
                            </li>
                        </ul>
                    </li> #}

                    <!-- User Account: style can be found in dropdown.less -->
                    <li class="dropdown user user-menu">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="font-size:14px;">
                            {% if app.user.photoDir %}
                                <img class="user-image" 
                                    src="{{ asset('uploads/generic/' ~ app.user.photoDir) }}"
                                    width="50"
                                    height="50"
                                    style="border-radius:200px"
                                    alt="Foto"
                                />
                            {% else %}
                                {% image '@BackendBundle/Resources/public/img/avatar.png' %}
                                 <img src="{{ asset_url }}" class="user-image" alt="User Image">
                                {% endimage %}
                            {% endif %}

                            <strong>{{ app.user.username }}/{{ app.user.entity.name }}</strong>
                        </a>

                        <ul class="dropdown-menu">
                            <!-- Menu Footer-->
                            <li class="user-footer">
                                <div class="">
                                    <div class="media">
                                        <div class="media-left">
                                            <a href="#">
                                                {% if app.user.photoDir %}
                                                    <img class="media-object" 
                                                        src="{{ asset('uploads/generic/' ~ app.user.photoDir) }}"
                                                        width="50"
                                                        height="50"
                                                        style="border-radius:200px"
                                                        alt="Foto"
                                                    />
                                                {% endif %}
                                            </a>
                                        </div>

                                        <div class="media-body" style="color: gray">
                                            <h4 class="media-heading">
                                                <strong>{{app.user.username}}</strong>
                                            </h4>
                                            {{app.user.email}}
                                        </div>
                                    </div>

                                    <br/>

                                    <div style="color: gray">
                                        <span class="fa fa-lock"></span>
                                        <a href="{{ path('fos_user_change_password') }}">Alterar palavra-passe</a>
                                        <br/>
                                        <br/>
                                        <span class="fa fa-user"></span>
                                        <a href="{{ path('administration_user_edit', {'id' : app.user.id}) }}">Minha conta</a>
                                    </div>
                                </div>
                            </li>

                            <li class="user-footer">
                                <div class="">
                                    <a href="{{ path('fos_user_security_logout') }}" class="btn btn-primary btn-flat">Terminar sess&atilde;o</a>
                                </div>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
